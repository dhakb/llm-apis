// refer to /mastra

// does loads of things out of the box lol
// ready to go setup, just focus on the product, gettings things done.
// creates Mastra instance, accepts agents with tools, workflows, can have/keep memory (own sql store),loggers, ready to go server.

// comes with nice ui and playground.